import React from 'react';
import IntroLoader from '../components/sections/IntroLoader';

const IntroLoaderPage: React.FC = () => {
    return <IntroLoader onComplete={() => { }} />;
};

export default IntroLoaderPage;